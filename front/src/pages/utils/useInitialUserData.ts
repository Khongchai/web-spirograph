import { User } from "../../classes/domain/userData/User";

export function useInitialUserData(): User | null {
  //TODO, return null for now
  // Check if token exist in browser
  // if token exist, call api to try to log in else return null
  // if login succeeds return user, else return null

  return null;
}
